---
- name: Docker | EE | APT | Install Prerequisits for APT
  apt: name="{{ item }}" state=present
  with_items:
    - apt-transport-https
    - ca-certificates
    - curl
    - software-properties-common

- name: Docker | EE | APT | Ubuntu
  include: os/ubuntu.yml
  when: ansible_distribution == 'Ubuntu'

- name: Docker | EE | APT | Debian
  include: os/debian.yml
  when: ansible_distribution == 'Debian'

- name: Docker | EE | APT | Remove old repo lxc-docker
  apt: name={{ item }} state=absent
  with_items:
    - lxc-docker
    - docker
    - docker-engine
    - docker-ce

- name: Docker | EE | APT | Install Docker Engine
  apt: name=docker-ee state=present
  notify: Docker | Restart Docker
